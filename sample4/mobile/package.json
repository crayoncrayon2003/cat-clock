{
  "name": "cat-clock-mobile",
  "version": "1.0.0",
  "description": "Mobile Cat Clock Widget",
  "scripts": {
    "sync": "cd .. && npm run sync",
    "ios": "npm run sync && npx cap sync ios && npx cap open ios",
    "android": "npm run sync && npx cap sync android && npx cap open android",
    "sync:all": "npm run sync && npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "typescript": "^5.9.3"
  }
}
